# History

## 2026-07-05 — Admin redesigned as a left sidebar (BT/RG style)
Replaced the admin top-nav with a **left sidebar** (`AdminLayout` = sticky sidebar + `<Outlet>`; `AdminApp` now a layout route; the old `<AdminNav>` stripped from every page). Structure: **Dashboard**, then **Operations → Utility** (Posts Approval, Reported Posts, Complaints, Wards — each with a live count badge), **Reports** (Visitors, Registered users, Posts [renamed from "Number of posts"]), **Settings** (Category Sequencing [was Home Order], Login Limits — now a **popup**, `LoginLimits.tsx`, replacing the Settings page). **＋ New Post** pinned bottom. **Removed** Leads + User View pages (contact-interest tracking stays — feeds Post analytics' "Contacted"). Dead files: `AdminNav/AdminLeads/AdminUserView/AdminSettings` now unimported (tree-shaken).

## 2026-07-05 — Back button + scroll restoration
Two navigation fixes: (1) in-app back arrows (ward page/directory, complaint detail, category + browse pages) now use a **smart back** (`useSmartBack` → `nav(-1)` with a fallback route) so they return to wherever the user came from — not a hardcoded page — and register as a POP so scroll can restore. (2) Added app-wide **scroll memory** (`lib/ScrollMemory.tsx`, mounted in App): saves scroll per `location.key`, restores on POP (retrying while async content loads), scroll-to-top on PUSH; `history.scrollRestoration='manual'`. Home now **caches its sections per city** (`HOME_CACHE`) so returning paints instantly and the saved scroll actually restores. Also: complaint list now shows the **poster their own pending** submission ("Awaiting review") so it doesn't look lost pre-approval, and complaint post/comment **re-login on a 401** (keeps the form, retries) instead of a raw "invalid token".

## 2026-07-05 — Ward Complaints is now an orderable Home section
Made the Ward Complaints entry a first-class home-sequence section (`special:complaints`) so it shows in Admin → Home Order (tagged "Civic") and can be dragged to any position — Home renders `WardComplaintsSection` at that spot instead of a fixed top slot. New `special` section type in `describeSection`/`defaultHomeSequence`, `homeSections` emits a marker (no listing query), boot migration prepends it to existing saved orders, admin sequence UI shows the "Civic" tag. (Complaints are still NOT postable from "+ Post" — only from inside a ward, by design.)

## 2026-07-05 — WhatsApp contact pre-fills a message
Contact → WhatsApp on a listing now opens with a pre-typed message via `wa.me/…?text=`: EN "Hi, I'm interested in *{title}*", HI "नमस्ते, मुझे *{title}* में इंटरेस्ट है।" (title bold, language from the UI). i18n `wa.interested`, in `ListingDetail.pick('whatsapp')`.

## 2026-07-05 — Ward Complaints reshaped: ward directory + multi-member + contact buttons
Per owner: entry is now a **ward directory** (list wards by name + address) → tap a ward → its board (add complaint + insta-style complaint cards). **Multiple ward members per ward** (`members[]`; ward-member check now matches any member's mobile). **No user mobile shown anywhere** — posters/commenters by name only; ward members reachable via a **contact button** (call/WhatsApp/copy, `MemberContact`) shown at the top of the ward + complaint. Member comment tag renamed **"Ward Member"**. New route `/:city/complaints/ward/:n` (`WardPage`); admin ward editor now takes address + multiple members. Verified live. See [COMPLAINTS.md](COMPLAINTS.md).

## 2026-07-05 — Ward Complaints civic board BUILT & deployed (Phase 1)
Shipped the full ward-scoped civic complaint board. New backend `complaints` module (wards/complaints/comments/agencies; agencies seeded), the disposition model (open→in_progress→resolved/forwarded/on_hold/…​ with mandatory public reason), pre-approval of every complaint + resident comment (official/member comments auto-approve), ward-member-by-mobile verification + Official badge, name capture + display (not mobile). Frontend: complaints home/post/detail pages + Home entry + admin Wards + Complaints-moderation tabs + `cmp.*` i18n. Verified end-to-end live (add ward → post → approve → resident-comment→moderate → member disposition). **Owner to do:** enter the Gondia wards in Admin → Wards. Phase 2 (push/Me-too/analytics/auto-archive) deferred. Spec: [COMPLAINTS.md](COMPLAINTS.md).

## 2026-07-05 — Ward Complaints civic board designed (not built)
Scoped a new civic complaint board (ward-scoped: residents post problems, ward members + public comment, poster marks resolved). Four decisions locked with the owner (login+name & show name not mobile; open member number; pre-approve every complaint+comment; verified ward-member accounts + badge). Full spec + data model in [COMPLAINTS.md](COMPLAINTS.md); rationale in [DECISIONS.md](DECISIONS.md). Separate module; reuses login/photos/moderation/push. Build deferred until owner says go.

## 2026-07-05 — Deployed: push opt-in capture + Sell-used section fix
Shipped to prod together. **Sell-used fix:** posts made without toggling "Selling" saved `sale_or_rent: ''` (schema default) not `'sale'`, so the "Sell — Used Items" section (which filtered `='sale'`) missed them while search still found them. Fixed by defining "for sale" as **`sale_or_rent ≠ 'rent'`** (home section + browse + OG count) — covers the legacy `''` rows with no migration — and making new sell posts write `'sale'` explicitly. Verified live: the section count went 0 → 1 (the CBR now shows). **Push capture** (below) also went live: `/push/*` endpoints up, `sw.js` served, vapid-key returns the public key.

## 2026-07-05 — Push opt-in capture BUILT (Android; sending + admin deferred)
Built the user-facing web-push subscription flow (not deployed). Backend `PushModule` (`push_subscriptions` collection + `/push/vapid-key|subscribe|unsubscribe|link`), VAPID keypair generated (public in code/env; private kept OUT of repo for the box `.env` later). Frontend: `public/sw.js`, `lib/push.ts`, `user/PushHost.tsx` soft-ask card ("Yes, notify me" / "Not now" + "Tap Allow on the next screen" helper), fired on **after-post** + **zero-result search**, guarded (Android/Chrome, permission=default, not-iOS-tab, not-admin, 3-day cooldown, 3-ask cap); subs link to the user on login. **Deferred:** the send side (`web-push` + `POST /admin/push/send`) and the admin panel ("in a few days"), + automated personalized triggers. Both build green. See [NOTIFICATIONS.md](NOTIFICATIONS.md).

## 2026-07-05 — Push-notification strategy designed (Android-first, not built)
Agreed to add Android web push (VAPID, free, no Apple account; iOS deferred). Designed the full **when + what**: permission via a two-step soft-ask (our card first, real browser prompt only on "Allow" — never cold, since an explicit block is permanent), fired at high-intent moments (after posting, zero-result search). Wrote the notification message catalog (transactional to posters, interest-based to seekers/employers/buyers, broadcast digest) with **English + Friendly-Hindi copy** and anti-spam/frequency rules. Phase-1 plumbing (SW + subscribe endpoint + `push_subscriptions` + admin broadcast) scoped but **not written or deployed**; automated sends are Phase 2. Full spec: [NOTIFICATIONS.md](NOTIFICATIONS.md).

## 2026-07-05 — Split Sell and Rent into separate home sections + browse pages
The old combined "Sell / Rent" section is now **two**: **Sell — Used Items** and **Rent**. New section-id type `sale:<sale|rent>` (post_type=sell + sale_or_rent); `describeSection`/home-loop/`SALE_META` handle it, `defaultHomeSequence` emits both, and a boot migration splices `ptype:sell` → `sale:sale`,`sale:rent` in any saved home order. Browse: `/gondia/sell` now = used items for sale only, new `/gondia/rent` = rentals (CategoryPage `isSaleBrowse`; App `CityChild` + backend RESERVED sets + OG `browseLabel` all learn `rent`; search DTO/service gain a `sale_or_rent` filter). Form: the Sell toggle now reads **"Selling (Used)"**. i18n `saleSec.sale`/`saleSec.rent`. Verified live: Rent section shows its 3 listings; Sell-used auto-hides until one exists.

## 2026-07-05 — Share button on "See all" (browse) pages
Top-right **share** icon on both browse/"See all" headers — `CategoryPage` (kind-based: job-seeker/opening/sell/happening) and `CategoryListing` (business categories). Shares the current section URL (`origin + pathname`) via the native Web Share sheet on mobile, WhatsApp fallback otherwise; the link unfurls to the section's existing branded OG card. i18n `common.share`.

## 2026-07-05 — Kind-aware ranking (freshness for transactional, popularity only for evergreen)
Ranking is no longer one-size-fits-all. `rankScore` (was `balancedScore`) now branches by kind: **transactional** listings (`job_seeker`, `job_opening`, `sell`, happening-**news**) rank by **freshness + daily jitter with NO popularity term** — because they go stale once hired/filled/sold/old, and lifetime views would bury fresh ones; **evergreen** (`business`, happening-**info**, `other`) keep popularity; **events** headline the happening feed **soonest-first** and **past events are dropped** (best-effort parse of the free-text `event_date` — ISO or written-month+year only, never on ambiguous slash dates). Section/browse totals discount hidden past events. Verified live: top job seekers flipped from high-view to newest. Phase-2 backlog: availability/expiry ("still looking?/sold/filled"), profile-completeness boost. See [DECISIONS.md](DECISIONS.md).

## 2026-07-05 — Duplicate-posting warning on the post form
Typing the contact number + tabbing out now checks for an existing posting with the **same number + same kind** that the poster can edit (`POST /listings/check-duplicate`, JWT; own postings, or all for admin — so no number-enumeration leak). If found, a modal lists them (title + status) with **View/Edit ↗** (opens each in edit view in a **new tab** — `/edit/:id` or admin `/admin/listings/:id`), **Open all N in edit**, and **Continue with new**. Runs only when logged-in/admin; dismissed numbers aren't re-warned. Verified: a number with 3 job openings returns all 3. See [POSTING.md](POSTING.md).

## 2026-07-05 — Admin Settings: tunable login gate (time + contacts)
New **admin Settings page** (`/admin/settings`) with the "ask-for-login" limits, stored in `app_config.login_gate` (`GET /settings/login-gate` public, `PUT /admin/login-gate` admin):
- **Time spent** (default 30 min) — cumulative active time before a non-logged-in visitor must register. Enforced **client-side** (`user/LoginGate.tsx`, mounted in `App`): accumulates visible-tab time in `localStorage.wl_active_ms`; on exceeding the limit shows a compulsory (non-dismissable) full-screen OTP login. Admin app + logged-in users exempt.
- **Contacts requested** (default 3) — distinct contact reveals before login. Enforced **server-side**: `reveal()` now reads `contact_limit` from the config (was the fixed `ANON_LIMIT` env) via a 60s-cached `getLoginGate()`.
- **Either** limit reaching forces the free register. **0 disables** that limit. Verified round-trip (set 45/5 → persist → read-back → restore 30/3). See [SECURITY.md](SECURITY.md).

## 2026-07-05 — Dashboard metrics (3 cards) + two drill-in analytics pages
- **Dashboard top row → 3 metrics** (was 5): Unique visitors · Registered users · **Number of posts** (new `total_posts` in the summary = live posts, approved+active = 45). All three cards click through.
- **Registered users page** (`/admin/registered`, `GET /analytics/registered-users`) — one row per OTP user, newest first: came-from, contacts requested, registered + first-visit dates, time spent, language, top interest, income tier. Row → the full activity tracker (visitor detail by user_id).
- **Post analytics page** (`/admin/posts`, `GET /analytics/posts`) — per live post: **landings** (sessions arriving via its shared link, matched by listing_id or path→/l/:id/slug), distinct **visitors**, **views**, and **contact requests**. Sorted by reach. (Top post: 319 landings / 316 visitors / 87 contact requests.)
- Refactor: lifted `bucketOf` + `CONTACT_PTS` (interest points model) to module scope in analytics.service so visitorDetail + registeredUsers share one implementation.

## 2026-07-05 — Job taxonomy audit: +4 employee roles, label/emoji polish, deeper synonyms
Deep review of the job taxonomy against a Gondia blue-collar market.
- **+4 employee-only roles** (31→35): Petrol Pump Attendant, Supervisor/In-charge, Recovery/Loan Agent, Farm/Agriculture Labour (khet mazdoor — moved the agri terms out of Gardener for a clean split). **Seeker↔owner boundary set:** self-employed trades (mobile/laptop repair, photographers) are deliberately **left in Business/Services**, not added as jobs; AC/appliance repair held out on the same principle.
- **Label/emoji polish for low-literacy:** deduped Plumber emoji (was 🔧 same as Mechanic → 🚰); Office Boy 📎→🧑‍💼 + label चपरासी; simplified hard Hindi labels (Welder फैब्रिकेशन→लोहा-ग्रिल काम, Factory प्रोडक्शन→कारखाना, Telecaller Latin "BPO"→कॉलिंग जॉब).
- **Deeper synonyms** (Roman/Hinglish — the ones that actually filter): ola/uber/school bus/ambulance (driver), zepto/blinkit/porter (delivery), gunman/armed guard (security), शिक्षक/montessori (teacher), molkarin/cook-cum-maid (maid).
- **⚠️ Found but NOT fixed (owner deferred):** Devanagari is unsearchable — `norm()` strips non-`[a-z0-9]`, so typing `बाई` returns all 42 listings instead of maids. Roman/Hinglish search works. Fix = keep `ऀ-ॿ` in `norm` + reindex + guard empty-after-strip. See [POSTING.md](POSTING.md).

## 2026-07-05 — Privacy fix (hidden name unsearchable), marital status, contact-leads feed, counter
- **Privacy: a hidden name is no longer searchable.** Typing "muskan" was surfacing a masked job-seeker ("M\*\*\* U\*\*\*") because the name was still folded into `search_blob`. Now the title is dropped from the search blob whenever `hide_title` is set (create + adminUpdate); findable by role/skills/category only. Existing hidden rows reindexed on the box. See [POSTING.md](POSTING.md).
- **Job-seeker `marital_status`** (Single/Married/Divorced/Widowed) — optional dropdown next to Gender, stored + shown on detail, bilingual. Type/DTO/schema/form/i18n all wired.
- **Contact-interest LEADS feed** (`/admin/leads`, `GET /analytics/leads`). Every `contact_click` (reveal/call/whatsapp/copy/share/cta) — already captured — is now surfaced as a post-centric feed: **who** acted on **which post's** contact and **how**, newest first. The basis for future billing on demand. See [ANALYTICS.md](ANALYTICS.md).
- **Visitors-today counter** was showing the raw `30` — the pm2 process had never been restarted with `INFLATE_BASE=613`/`INFLATE_MULTIPLIER=7.63` in env. A `pm2 restart --update-env` loaded them; now `floor(613 + real×7.63)` (real today ≈ 493 sessions / 333 visitors → ~4374). Tunable in the box `.env`.

## 2026-07-05 — Income tiering (all visitors) + deep job-role taxonomy
Two parallel builds (both green; deploy per owner's word).
- **Income / affluence tiering for every visitor (anonymous included).** Cheap-signal estimate → **High / Mid / Low**: `incomeSignal({os,brand,lang,topLabel})` in `analytics.service.ts` (base 35; Apple/iOS +40, Pixel/OnePlus/Nothing/iQOO +18, Samsung +10, generic Android +3, unknown/desktop +15; English UI +18; high-value interest +8 / low-value −8; clamp 0–100; ≥65 High, ≥38 Mid, else Low). UA parser (`device.ts`) now extracts a **handset `brand`** (Apple/Samsung/Xiaomi/Realme/Vivo/Oppo/OnePlus/Pixel/Nothing/iQOO/Motorola/Infinix/Tecno/Nokia/Android) — **model is NOT in the UA**, so Apple = "Apple". Shown as an **Income pill + brand** in the admin Visitors list and a 💰tier/score + 📱brand pill in visitor detail. Because the score rides on `visitor_id`, an **anonymous visitor already has a tier**; on OTP login `POST /events/identify` (JWT-guarded, `user_id` from the token) backfills their prior anonymous events with `user_id` (`updateMany({visitor_id, user_id null}, {$set:{user_id}})`) so the accumulated affluence + interest history follow them into their identified profile. Frontend `identify()` in `lib/analytics.ts`, called after login in `lib/userAuth.ts`. See [ANALYTICS.md](ANALYTICS.md).
- **Deep job-role taxonomy `JOB_CATEGORIES` (31 roles), same shape as `SELL_CATEGORIES`.** One taxonomy for both Hiring & Job-seeker, deep EN+Hinglish+Devanagari+local synonyms, wired through the shared `CategoryPicker` (required for jobs), search-blob synonym folding in `listings.service`, and role-as-eyebrow display. This is what lets the owner filter e.g. "house-help candidates" out of thousands. See [POSTING.md](POSTING.md).

## 2026-06-21 — Day 0
- Project created at `E:\DATA\DESKTOP\whatslocal2.0`, separate from the earlier `whatslocal` job-marketplace.
- Database `whatslocal2_0` + first collection `details` created on RG ERP's Atlas cluster.
- Conventions established: centralized name (`brand.config.json`), `docs/` living documents, RG ERP stack on a single EC2, served on a link.
- Product defined: a city-wide **local search engine / yellow pages** + jobs (see [VISION.md](VISION.md)).
- **Monorepo scaffolded & verified** — backend (NestJS) + web (React/Vite) + shared types build and run; backend connected to `whatslocal2_0`; pincode resolver returns Gondia; **analytics pipeline live** (events + summary, incl. zero-result searches).

## 2026-06-26 — Share cards, photos, balanced ranking, hide_number + fresh-start data wipe
A large batch focused on making shared links look great, letting posts carry photos, and making the feed fair to new listings.

- **Auto-generated WhatsApp/social share cards (OG) shipped for every listing.** nginx routes crawler bots to `GET /api/v1/og`, which renders `og:*` meta and a **1200×630 JPEG** card built from the listing's own data (`buildCardSvg`) — the eyebrow + highlight adapt per post type (sell **price**, rent **"₹X / period"**, job **salary range**, seeker **experience**, event **date**, news/info **label**, business **category**), title masked per hide-title, on brand teal with a **city + whatslocal.in** footer + amber bar. **Output is JPEG, not PNG** — WhatsApp silently drops over-large preview images (a 1200×630 photo is ~120 KB JPEG vs ~750 KB PNG; text cards ~83 KB). Cards are pre-rendered to a disk cache `/var/www/whatslocal-og/<id>.jpg` (nginx-served, `@oggen` Node fallback) on approve/create/edit/show, removed on hide/reject. Edited posts re-preview via a base36-`updatedAt` cache-buster (`?s=`/`?v=`) because WhatsApp caches per URL/format.
- **Browse/category cards** unfurl too: sharing `/gondia`, `/gondia/job-opening`, `/gondia/cat/<key>`, `/gondia/sell`… shows a branded card with the section label + a **live count** ("50+" once ≥ 10) + city (15-min cache). **Social pre-warm** (`prewarmSocial`, env-gated by `FB_SCRAPE_TOKEN`) pings Facebook's scraper on approve/edit so the very first share already has the full image; no-op without the token.
- **Listing photos.** `POST /listings/upload` (JWT) re-encodes ONE image with sharp into a `view`(≤1280,q82) + `thumb`(≤480,q75), **EXIF/GPS stripped**; up to 8 photos per post (`p`+16 hex keys in `photos[]`); files under `/var/www/whatslocal-media`, served by nginx `/media/`. Detail page = `object-contain` gallery; tiles = uniform square (photo or category icon). Photos with an uploaded cover now drive the share card (cover photo fit `contain` + city footer). Orphan sweep (boot + every 12h, 24h grace) reclaims abandoned uploads; key-format path-traversal guard on every file op.
- **Balanced default ranking** for browse + home rows: `log10(1+views)` + a **fresh-boost that fades weekly** (from `createdAt`, so re-posting can't game it) + a per-day jitter that rotates near-ties. New posts get real exposure without burying popular ones; text search still uses relevance ranking. In-memory over a 500-cap pool (one-city scale).
- **`hide_number`** (Happening news/info): number collected for OTP/ownership but never exposed — `toPublic` forces the contact flags off and `reveal()` refuses; the form guards the flag to Happening-only. This fixed a real bug that had hidden **Bharat Traders'** contact when the flag leaked across a post-type switch (fixed in data + code).
- **Posting-form polish:** photos on every type; Sell/Rent is **mode-driven** (Sell vs Rent first, categories filtered by mode, Property merged, Cars/Bikes rentable, rent adds `rent_period`); Happening reshapes per Event/News/Info (event date+venue, or mandatory sub-heading + body with hide_number defaulting on); the hiring form dropped the vacancies/openings field, moved Gender up top, made experience a yes/no→months toggle, and made office timing opt-in; the contact block is "Number for Calling" + "Number for WhatsApp" side-by-side with a "Same as calling" mirror.
- **Analytics detail** gained the **entry/landing page** (+ external referrer, resolved to the post when a listing), an interest **points model** (dwell + contact actions + own posts, bucketed by category/kind), and **latest language + current city**. The public "visitors today" counter is inflated server-side (`floor(613 + real×7.63)`; real count never leaves the box).
- **Fresh-start data wipe (production).** To launch on real data, all **listings / reveals / reports / mod-events** were cleared on the box; **users, tags, and config were kept**. A backup was saved on the box before the wipe. No schema/destructive migration — just a content reset so the balanced ranking and share cards start against genuine listings.

## 2026-06-25 — Sell/Rent categories (icons + keywords + search)
- Sell/Rent posts now get their own **category system**, mirroring business. New `SELL_CATEGORIES` (20: Cars, Bikes & Scooters, Cycles, Property for Sale, Property for Rent/PG, Mobiles & Tablets, TV/Fridge/Appliances, Computers & Laptops, Furniture, Home & Kitchen, Clothing & Fashion, Books & Stationery, Sports & Fitness, Kids/Baby/Toys, Pets & Livestock, Farming & Agri Equipment, Musical Instruments, Tools & Machinery, Scrap, Other) — each with `key` (`sell_*`), EN label, `labelHi`, emoji, and 10–20 EN+Hindi synonyms. `ALL_CATEGORIES = business + sell`; `CATEGORY_BY_KEY/LABEL` rebuilt from the merged set (business catalog kept for its own picker). `SUGGESTED_KEYWORDS` extended per sell key. Web: shared `CategoryPicker` component (business + sell), `searchSellCategories()`, sell category required in `validate()`, auto-keywords run for sell too. **Search:** synonyms fold into `search_blob` via the merged map (so "bike"/"makaan"/"kabad" find sell posts); cards/detail icon+eyebrow resolve via the merged maps. No backend source change (sell already flows the non-job category path) — types/dist redeployed + API restarted. NOTE: pre-existing sell posts won't have a category until re-saved.

## 2026-06-25 — Visitor "Interested in" = points model + latest lang/city
- **"Interested in" is now a points score** across all signals (was time-on-categorized-views only, so it showed "None" for users whose activity was on non-category posts like sell). Points: a `listing_view` = `3 + dwell-minutes×6` (capped); a `contact_click` = call/whatsapp 30, cta 20, share 15, copy 12, open 8; **what they post** = their own listings ×40 (strongest signal). Bucketed by **category** (business) OR **kind/post_type** fallback (Jobs / Sell-Rent / Happenings / Other) so every interaction counts. Ranked by points.
- **Language stat → latest selected** (`last_lang` = most recent event's lang) instead of the full set; updates as the user switches.
- **Current city** recorded + shown: the most-recent event's city is highlighted in the header as "📍 {city} · now" (other cities still listed). `visitorDetail` now returns `last_lang` + `current_city`.

## 2026-06-25 — WhatsApp link-unfurl (Open Graph) + Sell/Other sections + batch
- **Social link preview (OG) for crawlers — LIVE.** Backend `GET /api/v1/og` renders a tiny HTML page with `og:title/description/url` (and `og:image` + `twitter:card=summary_large_image` when the listing has a photo) for a listing resolved from the path (slug or `/l/:id`, approved+active only, masked title respected). nginx routes **bot user-agents** (WhatsApp/FB/Telegram/etc., via a `map`) to it using an `error_page 418 → @bot` trick that `rewrite`s to `/api/v1/og` and passes the original path in `X-Og-Path`; humans get the SPA. A media-extension `location` serves images/fonts/favicon directly (never bot-routed) so future photo URLs unfurl. **Photos plug in later** → the large thumbnail appears automatically once image upload exists (ensure `photos[0]` is an absolute URL or served from our domain with a file extension / an external host). Verified: WhatsApp UA → OG card, browser UA → app, `/logo.svg` → real file.
- **Sell/Other now list** (#2): `sell`/`other` posts are `kind=business` with no category → had no home section. Added `ptype:sell` + `ptype:other` sections (`PTYPE_META`, `describeSection`/`homeSections` handle `type:'ptype'`, `defaultHomeSequence` includes them, `search()` gained a `post_type` filter; CategoryPage + CityChild + Home handle the browse). NOTE: a user post is still **pending** until admin approval — that's why a fresh "Rent test" doesn't show yet.
- **Batch (#3–#9):** city switcher (header → pincode popup → that city's posts, dynamic cities remembered in localStorage); OTP watermark → "Enter OTP" everywhere; **captcha removed** app-wide (widget toggle off too); WhatsApp tick moved below the WhatsApp-number field + auto-ticks on entry; job-seeker "Work you do" → multi-line "Skills, Experience and Other Details" (risky gig examples removed); business option → "List business / profession / service.."; default post type → Jobs.

## 2026-06-25 — Full user-facing i18n (phase 2 done)
- The earlier passes left the **Post form** and **category labels** in English (phase-2 TODO). Now done: every Post-form field label, placeholder, helper, button, OTP-stage string, toast, success popup, error, and edit/admin header goes through `t()`; `SearchKeywords` and `Categories` translated too. ~110 new i18n keys (`post.*` etc.).
- **Business category labels are bilingual**: added `labelHi` to all 39 `BUSINESS_CATEGORIES` + `categoryLabelLocalized(keyOrLabel, lang)` in `@whatslocal/types`; the picker tiles, card/detail category eyebrow, and Home `cat` section labels render the localized label via `catLabel(..., lang)` (Home maps by `s.key` since the backend still returns the English label). Admin UI stays English by design. Minor: the SearchKeywords "Hidden from buyers" bold markup became plain text (t() returns a string).

## 2026-06-25 — Visitor explorer: group by person + time-weighted interest
- **One row per PERSON, not per device.** The Visitors list grouped by `visitor_id`, so a logged-in user with multiple devices/browsers showed up repeatedly. Now `visitors()` groups by `$ifNull(user_id, visitor_id)` — identified users merge all their devices into one row (anon stay per-device). The list returns `id` (the group key) for the drill-in; `visitorDetail(id)` queries `{$or:[{user_id:id},{visitor_id:id}]}` so it gathers all of an identified person's events across devices.
- **"Interested in" now by TIME SPENT, not raw views.** Per `listing_view`, dwell = time until the next event in the same session (min 15s, capped 300s); summed per category. `top_categories` returns `{label, seconds, count}` sorted by seconds; admin shows the time (e.g. `4m · 13×`). Better signal than a click count (a quick accidental tap ≠ interest).

## 2026-06-24 (later) — Slug privacy + detail-page value translation
- **Privacy: hidden names must not leak in the URL.** A `hide_title` listing (public name masked `R***`) was getting a name-based slug (`/gondia/Ramesh`). Now hidden-name listings get a **random slug** (e.g. `0znxqv5b2t`). Fixed in `uniqueSlug(title, excludeId, hidden)` (random base when hidden); applied on create, on edit (re-mints if a slug still leaks), and a **boot migration** re-minted existing leaking ones. Verified: `/by-slug/Ramesh` → 404, hidden rows now random. Relates to [[whatslocal2-security-mandate]].
- **Detail-page dropdown values now translate too.** The earlier i18n pass covered the form/filters but the **detail page** still rendered stored codes raw (`male`, `full_time`). Now gender / gender-required / job-type / happening-type map through the `opt.*`/`hap.*` keys (`male`→पुरुष in Hindi).

## 2026-06-24 (later) — Brand logo + favicon
- Added the **WhatsLocal logo** (teal location-pin + 3 white "search" dots over the wordmark) at `apps/web/public/logo.svg`, plus a pin-only square **`favicon.svg`** (extracted from the same art) wired in `index.html` (+ `theme-color #0F766E`). Logo shown on the **login** screen and the **first-visit language picker** (white bg). Not used in the teal Home header (teal-on-teal would be invisible — needs a white variant). Deployed.

## 2026-06-24 (later) — Dropdown values now bilingual
- Every user-facing **dropdown/select option** now translates with the EN/हिं toggle (was hardcoded English): Post form (post type, Sale/Rent, Gender, Gender-required, Job type, Happening-type buttons), CategoryPage job filters (gender/age/exp/salary), ListingDetail report reasons. Added `useT` to `Post.tsx` + `CategoryPage.tsx`; new `opt.*` / `ptype.*` / `hap.*` / `reason.*` / `filter.*` i18n keys. **Stored value codes unchanged** (only display translates). NOTE: the surrounding Post-form *field labels/placeholders/buttons* are still English — full Post-form i18n remains the phase-2 TODO.

## 2026-06-24 — Live + readable URLs
- **Deployed to production** on RG ERP's EC2 (co-tenant; own pm2 `wl-api` :9100, nginx vhost, `/var/www/whatslocal2`). Domain **https://whatslocal.in** linked (GoDaddy A/CNAME) with Let's Encrypt SSL + http→https. `trust proxy` enabled so per-user rate limiting is correct behind nginx. See [DEPLOYMENT.md](DEPLOYMENT.md).
- **UX + analytics batch** (built, not yet deployed):
  - Home visitor badge now reads "**{n} Local Visitors Today**".
  - **Preferred language persists** (already did: `setLang`→localStorage, gate only on first visit) — confirmed, no change needed.
  - **Posting WhatsApp number**: unchecking "WhatsApp" on the contact mobile now reveals an *optional* separate WhatsApp-number field (moved out of "More"). Detail page's WhatsApp button uses that separate number when present.
  - **Listing detail action row**: Share / Home / More (← category for businesses, kind page for jobs/events).
  - **Share** composes a rich WhatsApp-ready message (icon + title + sub + category + area + link) via Web Share API / `wa.me`. (Link *unfurl* card — OG meta + bot routing — deferred; needs a deploy.)
  - **Deeper analytics capture**: every event now carries `lang` + `user_id`; a RouteTracker logs a `page_view` per navigation (path trail + timestamps for dwell); on OTP login `POST /events/identify` backfills the visitor's prior anonymous events with their user_id (identity-linking).
  - **Real OTP via MSG91 (headless widget)** — ✅ **tested working end-to-end 2026-06-24** (Demo Credentials, captcha off): widget → access-token → `verifyAccessToken` → session; backend extracted the correct number. Two gotchas learned: (1) widget exposes `sendOtp`/`verifyOtp` a beat *after* `initSendOTP` — must poll for them; (2) **headless mode doesn't render MSG91's hCaptcha**, so captcha must be OFF in Widget Settings (or rendered inline via `captchaRenderId` — production TODO). **Real SMS confirmed working without DLT** (Channels Config → SMS → "Use Default Configuration" = MSG91 shared template route; no DLT, generic sender). Earlier non-delivery was Demo-Credentials override + account KYC, not DLT. Wired + env-gated (falls back to demo `1234` until keys are set). Go-live remaining: re-enable captcha (render inline) + deploy. Frontend `lib/msg91.ts` lazy-loads `verify.msg91.com/otp-provider.js`, `initSendOTP({widgetId, tokenAuth, exposeMethods:true})`, drives `sendOtp/verifyOtp` behind our own `OtpLogin` UI; on verify it gets a signed access-token. Backend `POST /auth/otp/widget-verify` + `/auth/number/widget-verify` confirm the token via MSG91 `verifyAccessToken` (using `MSG91_AUTH_KEY`) and read the **verified** number from the response/JWT (never trusts a client-supplied number). Env: web `VITE_MSG91_WIDGET_ID` + `VITE_MSG91_TOKEN_AUTH` (public), backend `MSG91_AUTH_KEY` (secret). Channel chosen: SMS (needs DLT-approved template/sender on MSG91). **Pending**: real keys → set env, confirm `verifyAccessToken` response shape with a live token, deploy.
  - **Auto-suggested keywords**: picking a business sub-category now pre-fills ~6 curated, most-searched keywords for it into the keyword box (union across up to 3 categories, deduped, capped at 25). Removing a category pulls its words back out unless another selected category shares them; manual additions/removals are preserved. Edit-load is seeded so it never re-pads an existing listing. Data: `SUGGESTED_KEYWORDS` + `suggestedKeywordsFor()` in `@whatslocal/types`. An amber hint tells the lister they can edit freely.
  - **Admin user-explorer** (#5 done): new **Visitors** tab. `GET /analytics/visitors` lists every visitor (one row per `visitor_id`, newest first, identity attached when known: mobile, events, views, contacts, searches, sessions, lang, device, city). `GET /analytics/visitors/:visitorId` returns deep detail — identity, time-engaged, languages, devices, **categories they browse most**, **contact/share actions** (with listing links), **searches** (incl. zero-result), and a full **activity timeline**. Dashboard "Unique visitors" stat links in. (`AnalyticsModule` now imports `AuthModule` + the `Listing` model to resolve mobiles & listing titles.)
- **Readable listing permalinks** — every listing now has a unique `slug` derived from its title; public URL is `/{city}/{Title_Of_Listing}` (e.g. `/gondia/Sharma_General_Store`) for local feel + shareability. Slug is **stable** (kept on title edits so links never rot), unique (auto `_2`, `_3` on collision, partial-unique index + create retry), case-insensitive lookup, accents folded to ASCII. Legacy `/l/:id` still resolves. One route `/:city/:kind` dispatches: known kind slug → category page, else → listing detail. Backend: `GET /listings/by-slug/:slug`, idempotent slug backfill on boot.
