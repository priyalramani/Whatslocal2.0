# Copy to .env (or .env.production) and fill in before building the web app.
# These two are PUBLIC (safe to ship in the frontend bundle). When unset, the
# app falls back to the backend demo OTP (1234).
VITE_MSG91_WIDGET_ID=
VITE_MSG91_TOKEN_AUTH=
